# 数据看板与统计分析子系统 · 基础平台接入与鉴权设计方案

| **文档信息** |  |
| --- | --- |
| **文档版本** | V0.1 设计草案 |
| **所属系统** | 数据看板与统计分析子系统（测评业务全生命周期管理系统） |
| **编制日期** | 2026-08-17 |
| **文档状态** | 草案，待评审；文中 `待确认` 项须在评审会与 PoC 中闭环 |
| **依据文档** | 《需求规格说明书 V1.3 定稿版》《页面清单 V0.3》《指标字典（模板）》《Metabase 合同看板 PoC 执行计划》《新子系统开发对接交付清单 V1.1》《应用授权目录同步说明》《系统架构》《核心模型与MySQL设计》《平台接入说明》 |
| **技术基线** | 基础平台（OIDC/Keycloak + 授权目录 + 审计 ingest + 配置命名空间 + 统一前端）；客户与商机子系统（Go + Gin + GORM + Temporal + 多 worker + Vue 3 模块） |

---

## 0. 设计目标与原则

### 0.1 目标
1. 以**标准子系统身份**接入基础平台：统一认证（OIDC）、授权目录发布、审计上报、配置托管、统一前端模块，满足接入手册全部契约。
2. **技术栈严格对齐客户与商机子系统**：Go 服务 + 多 cmd 二进制 + 独立 MySQL + Vue 模块，最大程度复用既有工程模式。
3. 实现可落地的**功能级权限 + 行级数据范围**（老板全量 / 销售总监本人及下属 / 技术总监管辖团队 / 财务全量）。
4. 满足等保与数据安全要求：数据不出系统、不导出、不邮件、操作留痕。

### 0.2 设计原则
- **P1 平台为唯一身份与授权源**（NG7）：不另建认证/权限；Keycloak 只签 Token，业务授权以平台目录镜像为准。
- **P2 行级数据范围子系统自研**：平台只负责角色/权限分配（ScopeType = tenant/environment），业务行级范围由看板系统在服务层实现（合同 manifest 语义先例）。
- **P3 看板查询只命中聚合库**（Q5 固化时段）：白天 7:00–23:50 不触碰业务源库；夜间 1:00–06:00 完成同步与预计算。
- **P4 口径唯一来源是指标字典**：所有聚合 SQL 与嵌入参数以字典为准，禁止 BI 侧自行写死计算。
- **P5 复用优先，新增最小化**：平台能力（OIDC/目录/审计/配置/通知）与客户与商机工程模式全部复用；仅新增 3 个平台模型之外的必要组件：Metabase 第三方服务、跨库同步层、嵌入桥。

### 0.3 与既有文档的两处偏差修正（需评审确认）
1. **NG2 表述修订**：由"不建数据仓库/数据中台"修订为"不建独立数仓/中台，但建设**轻量 T+1 聚合库**（维表镜像 + 预计算聚合）"——跨 MySQL 实例无法用视图实现 JOIN（见 §5.1 依据），必须有一个只读聚合落点。
2. **"复用基础平台规则配置能力"修订**：平台 configuration 域只能托管**配置项**；预警**规则判定逻辑**由看板系统 `alert-worker` 自建（见 §6）。

---

## 1. 总体架构

```mermaid
flowchart LR
    U["浏览器/门户"] --> F["统一前端 (Vue 3, frontend/src/modules/data_analysis)"]
    F -->|"OIDC PKCE + Cookie 会话"| D["dashboard-api (Go 嵌入桥+业务 API)"]
    F -->|"iframe: /data_analysis/api/v1/embed-proxy/{token}"| D
    D -->|"只读 SELECT"| M[("dashboard-mysql 聚合库")]
    D -->|"签名嵌入 + 参数注入"| MB["Metabase (内网, 仅 API 交互)"]
    D -->|"OIDC / 目录同步 / 审计 ingest / 配置"| P["基础平台 (Keycloak + 授权目录 + audit + cfg)"]
    D -->|"UserInfo / 组织任职数据(待确认接口)"| P
    A["aggregation-worker (夜间跨库同步+预计算)"] -->|"只读账号(夜间窗口)"| S1["合同库"] S2["客户与商机库"] S3["项目库"] S4["报告库"] S5["结算库"] S6["报销库"]
    A -->|"写入"| M
    W["alert-worker (预警计算)"] --> M
    W --> D
    SY["user-sync / catalog 发布"] --> P
    MB -->|"查询"| M
```

### 1.1 组件清单

| 组件 | 类型 | 职责 | 技术基线 |
|---|---|---|---|
| `dashboard-api` | Go 服务 | OIDC 会话、权限/数据范围判定、嵌入桥（签发+代理）、预警中心/字典/管理 API、审计上报 | 对齐 CRM `crm-server` |
| `aggregation-worker` | Go 服务 | 夜间跨库同步（维表镜像）+ T+1 预计算聚合 + 同步状态管理 | 对齐 CRM `*-worker` + Temporal |
| `alert-worker` | Go 服务 | 按规则在聚合库计算预警快照（幂等） | 对齐 CRM `*-worker` |
| `authz-catalog` | Go CLI | 授权目录发布（平台镜像） | 直接复用 CRM `cmd/authz-catalog` 模式 |
| `dashboard-mysql` | MySQL 8.4 | 聚合库：维表镜像/聚合事实表/预警快照/同步元数据 | 平台规范：独立实例 + 版本化迁移 |
| `metabase` | 第三方 OSS | 看板渲染引擎（决策 A） | Docker 自托管，仅内网 |
| `frontend/src/modules/data_analysis` | Vue 3 模块 | 预警中心、指标字典、管理页 + 看板 iframe 壳 | 统一前端模式 |
| 业务子系统库 ×6 | MySQL 8.4 | 只读数据源（夜间窗口） | 各子系统提供只读账号 |

### 1.2 为什么是"嵌入代理"而不是"浏览器直连 Metabase"
- OSS Metabase 无 SSO、无行级沙箱；签名嵌入 URL 本质是"公开"端点，直接暴露会被分享/滥用。
- 因此 **Metabase 只在内网可达**，浏览器一律访问 `/data_analysis/api/v1/embed-proxy/{token}`，由 `dashboard-api` 代鉴权、代理、限流并记录审计。浏览器永远拿不到 Metabase 的地址与凭据。

---

## 2. 技术栈与代码结构（严格对齐客户与商机）

### 2.1 技术栈对照

| 维度 | 客户与商机（基线） | 本子系统 | 说明 |
|---|---|---|---|
| 语言/框架 | Go 1.25 + Gin | 同 | `go.mod` 按 CRM 依赖裁剪 |
| ORM/驱动 | GORM + go-sql-driver/mysql | 同 | |
| 认证 | go-oidc/v3 + golang.org/x/oauth2 | 同 | 复用 CRM `internal/middleware/auth.go` 三件套（SessionAuth / RequireSameOriginWrite / MachineAuth） |
| 会话 | Cookie（`<app>_session`，HttpOnly，服务端校验） | 同 | 独立 Cookie 名 `data_analysis_session`，Path=`/data_analysis/` |
| 进程形态 | 多 cmd 二进制 + 单 Dockerfile 全量构建 | 同 | `dashboard-api / aggregation-worker / alert-worker / authz-catalog / migrate` |
| 长流程/定时 | Temporal SDK（合同深度用，CRM 部分用） | 同 | 夜间批处理用 Temporal Workflow：`SyncAll → PrecomputeAll → BuildAlerts` |
| 审计投递 | `shared/requestaudit` poll 批量投递 | 同 | outbox + 幂等，独立审计机器 Client |
| 目录发布 | `cmd/authz-catalog` + `platformcatalog` | 同 | manifest 见 §3.4 |
| 前端 | Vue 3 + vue-router + Vite 模块 | 同 | `frontend/src/modules/data_analysis/` |
| 数据库 | 独立 MySQL 8.4 + 版本化 SQL 迁移 | 同 | `dashboard-mysql` |
| 集成 HTTP | `shared/integrationhttp` | 同 | 平台组织数据、Metabase API |

### 2.2 目录结构草案

```text
data_analysis/
├── cmd/
│   ├── dashboard-api/          # 嵌入桥 + 业务 API
│   ├── aggregation-worker/     # 夜间同步 + 预计算（Temporal worker）
│   ├── alert-worker/           # 预警计算
│   ├── authz-catalog/          # 目录发布（复用 CRM 模式）
│   ├── local-migrate/ production-migrate/
│   └── ...
├── internal/
│   ├── bootstrap/              # 配置装配（对齐 CRM）
│   ├── middleware/             # auth / request_audit / request_id / observability
│   ├── shared/                 # auth / requestaudit / integrationhttp / response ...
│   ├── platformcatalog/        # 目录发布（复用 CRM）
│   ├── embedbridge/            # 嵌入桥：令牌签发、代理、Metabase 客户端
│   ├── aggregation/            # 跨库同步 + 预计算（源适配器 + 写聚合库）
│   ├── alerting/               # 规则引擎 + 预警快照
│   ├── modules/
│   │   ├── overview/ contract/ project/ report/ finance/   # 各看板聚合查询
│   │   ├── alerts/             # 预警中心 API
│   │   ├── dictionary/         # 指标字典 API
│   │   └── admin/              # 预警规则配置、数据源状态
│   └── orgdirectory/           # 平台组织/任职数据客户端（对齐 CRM ownerdirectory）
├── migrations/                 # 聚合库版本化 SQL
├── authz/
│   └── permission-manifest.yaml
├── Dockerfile                  # 单 Dockerfile 多二进制（对齐 CRM）
├── compose.yaml                # dashboard-api/dashboard-mysql/metabase/workers
└── .env.example
```

---

## 3. 基础平台接入设计

### 3.1 应用登记（评审会确认后填写）

| 字段 | 建议值 |
|---|---|
| `application_code` | `data_analysis` |
| `application_name` | 数据看板与统计分析 |
| `environment` | dev → test → staging → prod |
| `public_base_url` | 平台门户 origin（如 `http://localhost:8081`） |
| `path_prefix` | `/data_analysis` |
| `upstream_url` | `http://dashboard-api:8080`（Docker 网络） |
| `target_uri` | `/data_analysis/` |
| `redirect_uri` | `<public_base_url>/data_analysis/auth/callback` |
| OAuth Client 类型 | confidential |
| 前端承载模式 | **统一前端模式**（Vue 模块，对齐合同/CRM） |

> 平台自动派生：浏览器 Client = `data_analysis-<env>-web`；目录同步 Client 与审计 Client 各自独立（接入手册 §2.1/§9）。

### 3.2 认证链路

```text
门户 → /data_analysis/ → GET /data_analysis/auth/login（生成 state/nonce/PKCE）
→ Keycloak 授权页（已有 Realm 会话则无感）→ 回调 /data_analysis/auth/callback
→ 校验 iss/aud/sig/exp/nonce/token_use + sub/tenant_id/roles/permissions/role_config_hash/authz_revision
→ dashboard-api 建立 HttpOnly Cookie 会话（data_analysis_session，TTL ≤ Token TTL）
→ 前端调 /data_analysis/api/v1/auth/me
```

- 严格遵守接入手册 §3 全部清单：PKCE S256、state 单次消费、拒绝未知/跨应用权限、不兼容目录哈希拒绝、登出撤销服务端会话、写请求同源 + `X-CSRF-Token` 校验（复用 CRM `RequireSameOriginWrite`）。
- 授权变更生效：定期 UserInfo 在线复核 + `role_config_hash`/`authz_revision` 变化销毁本地会话。

### 3.3 会话与身份映射
- 业务表统一保存：`tenant_id`、`platform_user_id`（OIDC `sub`）、`created_by/updated_by`、时间戳（接入手册 §4）。
- 展示名可存快照，业务外键一律用 `sub`。
- 聚合库所有表带 `tenant_id`，所有查询强制租户过滤（manifest `tenant_isolation: REQUIRED`）。

### 3.4 授权目录（manifest 草案，格式对齐合同 `authz/permission-manifest.yaml`）

```yaml
api_version: authz.data-analysis/v1
kind: PermissionManifest

metadata:
  application_code: data_analysis
  name: 数据看板与统计分析
  version: 1
  owner: data_analysis
  description: 看板查看、预警、字典与管理权限；行级数据范围由 dashboard-api 服务层强制实现

semantics:
  default_decision: DENY
  permission_match: EXACT
  tenant_isolation: REQUIRED
  superuser_wildcard: UNSUPPORTED
  notes:
    - 权限码必须精确匹配；后端所有看板/预警/字典接口按本清单鉴权。
    - 角色聚合权限；最终授权必须同时满足权限、数据范围和业务状态约束。
    - Metabase 不参与鉴权：行级范围由嵌入桥注入参数，Metabase 组权限仅收敛到"只读聚合视图 + 禁下载"。
    - 预警规则配置（alert.manage）写入平台 cfg 命名空间，配置变更必须审计。

permissions:
  - code: dashboard.overview.view
    name: 查看经营总览
    resource: dashboard_overview
    action: view
    risk_level: LOW
    enforced_by: [GET /data_analysis/api/v1/embed/overview]
    constraints: [仅返回当前租户数据；范围按角色数据范围收敛]

  - code: dashboard.contract.view
    name: 查看合同看板
    resource: dashboard_contract
    action: view
    risk_level: LOW
    enforced_by: [GET /data_analysis/api/v1/embed/contract]
    constraints: [仅返回当前租户数据；范围按角色数据范围收敛]

  - code: dashboard.project.view
    name: 查看项目执行看板
    resource: dashboard_project
    action: view
    risk_level: LOW
    enforced_by: [GET /data_analysis/api/v1/embed/project]

  - code: dashboard.report.view
    name: 查看报告与质量看板
    resource: dashboard_report
    action: view
    risk_level: LOW
    enforced_by: [GET /data_analysis/api/v1/embed/report]

  - code: dashboard.finance.view
    name: 查看财务与经营看板
    resource: dashboard_finance
    action: view
    risk_level: MEDIUM
    enforced_by: [GET /data_analysis/api/v1/embed/finance]
    constraints: [产值/成本数据仅对财务视角角色开放]

  - code: alert.view
    name: 查看预警中心
    resource: alert
    action: read
    risk_level: LOW
    enforced_by: [GET /data_analysis/api/v1/alerts]

  - code: alert.manage
    name: 配置预警规则
    resource: alert_rule
    action: manage
    risk_level: HIGH
    enforced_by: [GET/PUT /data_analysis/api/v1/alert-rules]
    constraints: [规则阈值写入平台 cfg；变更必须审计]

  - code: dictionary.view
    name: 查阅指标字典
    resource: metric_dictionary
    action: read
    risk_level: LOW
    enforced_by: [GET /data_analysis/api/v1/dictionary]

  - code: aggregation.manage
    name: 管理数据源与同步
    resource: aggregation
    action: manage
    risk_level: HIGH
    enforced_by: [GET /data_analysis/api/v1/admin/sources, POST /data_analysis/api/v1/admin/sources/{id}/trigger]
    constraints: [仅系统管理员；触发同步记录审计]

roles:
  - code: boss
    name: 老板/总经理
    permissions: [dashboard.overview.view, dashboard.contract.view, dashboard.project.view, dashboard.report.view, dashboard.finance.view, alert.view, dictionary.view]
  - code: sales_director
    name: 销售总监
    permissions: [dashboard.overview.view, dashboard.contract.view, dashboard.finance.view, alert.view, dictionary.view]
  - code: tech_director
    name: 技术总监
    permissions: [dashboard.overview.view, dashboard.project.view, dashboard.report.view, alert.view, dictionary.view]
  - code: finance_manager
    name: 财务经理
    permissions: [dashboard.overview.view, dashboard.contract.view, dashboard.finance.view, alert.view, dictionary.view]
  - code: dashboard_admin
    name: 看板系统管理员
    permissions: [alert.manage, aggregation.manage, dashboard.overview.view, dictionary.view]

data_scopes:
  - code: TENANT
    name: 全量数据
    applies_when: { role: [boss, finance_manager, dashboard_admin] }
    predicate: subject.scope == "tenant"
  - code: ORG_SUBTREE
    name: 本人及下属组织
    applies_when: { role: [sales_director] }
    predicate: subject.org_unit_id IN (self + descendants)，组织层级来自平台组织/任职数据（待确认接口）
  - code: TEAM
    name: 管辖团队
    applies_when: { role: [tech_director] }
    predicate: subject.team_ids == 任职管辖团队集合

subject_claims:
  required: [sub, tenant_id, roles, permissions, role_config_hash, authz_revision]

external_scopes:
  - code: audit.ingest
    principal_type: SERVICE
    purpose: 看板系统上报访问与配置审计
    assignment: 独立机器 OAuth Client
  - code: authorization.catalog.sync
    principal_type: SERVICE
    purpose: 发布授权目录
    assignment: 独立 catalog-publisher Client

compatibility:
  platform_catalog_must_include: [dashboard.overview.view, dashboard.contract.view, dashboard.project.view, dashboard.report.view, dashboard.finance.view, alert.view, alert.manage, dictionary.view, aggregation.manage]
  known_gaps:
    - 平台不承载业务行级范围；数据范围由 dashboard-api 基于平台组织数据自研实现
    - OSS Metabase 无 SSO/无沙箱；依赖嵌入桥统一鉴权（见 §4.3）
```

### 3.5 审计接入
- 事件清单（outbox 投递 + 幂等键，对齐 CRM `requestaudit`）：
  - `dashboard.access`（看板打开，含 dashboard_code + 范围摘要）
  - `embeds.issue`（嵌入令牌签发/代理，含 token_id、dashboard_code、TTL）
  - `alerts.rule.change`（预警规则阈值变更，含前后值）
  - `aggregation.trigger`（手动触发同步）
- 审计事件不包含：Token、Cookie、跨库凭据、明细数据。

### 3.6 配置托管（预警阈值）
- 平台 cfg 命名空间 `data_analysis.alert_rules`，配置项按规则：`{rule_code}.enabled`、`{rule_code}.threshold` 等。
- `dashboard-api` 启动/变更时读取并缓存，`alert-worker` 每晚计算前取最新值；配置变更写审计。

### 3.7 通知（可选能力，评审会定）
- 平台站内信可承接"预警产生"的通知（PRD 仅要求看板内标记，不邮件）。一期**不接入**，列为二期可选项，避免扩大范围。

---

## 4. 鉴权与权限分配设计

### 4.1 总体判定模型

```text
请求 → 会话鉴权（Cookie → principal）→ 权限判定（manifest EXACT 匹配 + tenant 过滤）
→ 数据范围判定（角色 → TENANT/ORG_SUBTREE/TEAM）→ 返回数据 / 嵌入 URL / 403
```

### 4.2 功能级权限 → 角色 → 数据范围矩阵

| 角色 | 可看页面 | 数据范围 | 越权处理 |
|---|---|---|---|
| boss | 全部看板 + 预警中心 + 字典 | TENANT 全量 | 后端强制，前端不提供越权选项 |
| sales_director | 总览/合同/财务 + 预警 + 字典 | ORG_SUBTREE（本人及下属） | 范围参数由服务端计算注入 |
| tech_director | 总览/项目/报告 + 预警 + 字典 | TEAM（管辖团队） | 同上 |
| finance_manager | 总览/合同/财务 + 预警 + 字典 | TENANT（财务视角） | 同上 |
| dashboard_admin | 预警配置/数据源管理 + 总览 | TENANT（仅配置面） | 无业务明细访问 |

> 与页面清单 OQ4 的"全局筛选默认值"完全一致；筛选器本身不提供越权选项（NG7 由平台角色映射收敛）。

### 4.3 行级数据范围四层防线

1. **嵌入桥（唯一计算点）**：`GET /data_analysis/api/v1/embed/{dashboard}` 校验权限 → 按角色从平台 **owner-directory**（`GET /internal/owner-directory`，scope=`owner_directory.read`，机器 Client；OQ-D1 已确认存在，能力边界见注记）计算范围 → 生成 Metabase 签名嵌入 URL 并**注入范围筛选参数**（如 `?param_sales_org=...`、`?param_team_ids=...`）→ 返回短 TTL 一次性令牌。
   > 注记（OQ-D1 能力边界，2026-08-17 核实）：owner-directory 按 user_id/keyword/role_codes 查询用户及其组织列表，**无组织树父子层级、无按组织查成员**。ORG_SUBTREE（本人及下属）实现候选：①平台扩展 owner-directory（按 org_unit_id 查成员 + org 层级）；②一期降级为“本人 org 全部用户”（OQ-D6 业务确认）；③看板系统本地基于 owner-directory 分页全量拉取建 org→users 映射（不含层级，仅平级）。
2. **嵌入代理（强制收敛）**：前端 iframe 指向 `/data_analysis/api/v1/embed-proxy/{token}`；`dashboard-api` 校验令牌（单次消费、TTL ≤5min、绑定用户）后反向代理到内网 Metabase。浏览器不接触 Metabase 本体。
3. **Metabase 侧（兜底）**：只读账号仅授聚合库视图/表；组权限设为"仅可用已保存问题"、禁下载、禁公开分享；Metabase admin 账号仅 API 使用并隔离。
4. **后端 API（预警/字典/明细下钻）**：全部按同一范围过滤，服务端强制，前端不做鉴权。

### 4.4 嵌入桥详细设计

| 子能力 | 设计 |
|---|---|
| 令牌生命周期 | `issue`（embed 请求）→ `consume`（proxy 首次请求）→ 过期（5min）；Redis 不可用时落 MySQL 状态表（对齐平台"不依赖 Redis"约束） |
| 范围参数注入 | 桥从组织数据算出 `{sales_org_subtree, team_ids, tenant_id}` 写入签名 token 的 filter 参数（Metabase signed embed 支持参数烘焙） |
| Metabase 用户策略 | 一期**不建用户**（纯签名嵌入 + 代理）；二期如需 Metabase 原生收藏/保存，再评估用户同步（见 OQ-D3） |
| 幂等与限流 | proxy 单令牌单次消费 + 网关级限流；异常令牌一律 403 |
| 审计 | 签发与访问各记一条审计 |

### 4.5 租户隔离
- 聚合库所有表带 `tenant_id`；嵌入参数与所有 API 查询强制 `tenant_id = 会话 tenant`；manifest `tenant_isolation: REQUIRED`。

---

## 5. 数据层设计（聚合库与跨库同步）

### 5.1 依据与边界
- 已核实：合同/项目/客户商机分属**独立 MySQL 实例**（`platform/compose.local.yaml`：`contract-mysql`、`project-mysql`、共享 `mysql`），MySQL 视图无法跨实例，Metabase Question 无法跨库 JOIN → **必须有聚合落点**。
- 边界：聚合库**不是数仓/中台**——只镜像看板所需维表 + 预计算聚合结果，不做明细存储与自助建模（NG1/NG2 精神不变）。

### 5.2 聚合库 Schema 草案（`migrations/` 版本化落地）

```sql
-- 同步元数据
sync_source (
  id, subsystem_code, db_host, db_schema, read_account_ref, enabled,
  last_watermark, last_run_at, last_status, last_error, updated_at
)

-- 维表镜像（夜间增量/全量刷新，幂等）
dim_customer (tenant_id, customer_id, customer_name, region_province, region_city, customer_category, ...)
dim_project (tenant_id, project_id, project_name, lifecycle_status, team_id, pm_user_id,
             actual_start_date, plan_start_date, contract_sign_date, archive_date, ...)
dim_org_user_snapshot (tenant_id, user_id, org_unit_id, position_id, role_type[tech/sales], valid_from, valid_until)

-- 聚合事实表（T+1 预计算，口径严格按指标字典）
fct_contract_signing (tenant_id, period_month, region, customer_category, sales_director_id, sales_id,
                      sign_amount, contract_count, discount_rate_bucket, ...)      -- 字典 1.1-1.5
fct_delivery_cycle   (tenant_id, period_month, team_id, pm_user_id, avg_days, p50_days, p90_days, count, ...)  -- 字典 2.2/2.6
fct_project_status   (tenant_id, snapshot_date, status, count, ...)                -- 字典 2.1
fct_report_quality   (tenant_id, period_month, team_id, author_id, cycle_days, first_pass, return_reason, complaint, ...) -- 字典 3.1-3.4
fct_finance          (tenant_id, period_month, dept_type, dept_id, person_id, output_value, cost, aging_bucket, unpaid_top, ...) -- 字典 4.1-4.7

-- 预警快照（alert-worker 每晚计算）
alert_item (tenant_id, alert_type, rule_code, severity, target_ref, title, due_date,
            status[open/ack/closed], created_at, closed_at, UNIQUE(tenant_id, alert_type, target_ref))
```

### 5.3 同步策略（aggregation-worker，Temporal Workflow）

| 项 | 设计 |
|---|---|
| 窗口 | 夜间 1:00–06:00（Q5 固化时段）；白天 7:00–23:50 聚合库只读 |
| 方式 | 源库只读账号按表增量拉取（`updated_at/created_at` 水印）；小表全量 TRUNCATE+LOAD |
| 幂等 | `INSERT ... ON DUPLICATE KEY UPDATE` + 唯一键；失败重试（指数退避）后状态入 `sync_source` |
| 口径 | 维表 JOIN 与聚合 SQL 只发生在聚合库；每条聚合表对应指标字典条目并记录 `metric_dict_version` |
| 完成判定 | 全部同步任务成功 + 预计算完成 + 预警计算完成；任一失败触发告警（站内信二期）并保留昨日数据 |
| 手工触发 | `POST /data_analysis/api/v1/admin/sources/{id}/trigger`（dashboard_admin，审计） |

### 5.4 只读账号治理（跨团队运维契约，接入手册未覆盖——评审重点）
- 每个业务子系统提供**独立只读账号**：仅 `SELECT` 看板所需表/视图，连接数上限，语句超时（中间代理层实现）。
- 凭据经部署 Secret 管理（`0600`、不入 Git、不入日志），仅在 `aggregation-worker` 夜间运行期使用。
- 验收口径：白天主库 QPS/IOPS 增幅 <5%、无交易抖动（对齐 PoC V4 判据）。

---

## 6. 预警中心设计

| 项 | 设计 |
|---|---|
| 规则模型 | `alert_rule`（code、来源聚合表、判定条件模板、severity、enabled）；阈值参数放平台 cfg（§3.6） |
| 计算 | `alert-worker` 每晚在聚合库执行规则 → upsert `alert_item`（幂等，按 `target_ref`） |
| 展示 | 预警中心页（前端模块）调 `GET /data_analysis/api/v1/alerts`（类型/严重度/到期日排序，红标置顶）；各看板内高亮由聚合表状态字段驱动 |
| 闭环 | `open → ack → closed`（ack/closed 需 `alert.manage`；写入审计） |
| 非目标 | 不邮件、不自动外发（NG5）；不做预测评分（NG4） |

---

## 7. Metabase 部署与安全

| 项 | 设计 |
|---|---|
| 部署 | `compose.yaml` 内第三方服务（2C4G 起），版本固定 + 健康检查 + 备份策略；**不登记为子系统、不对外暴露** |
| 网络 | 仅内网：`dashboard-api` ↔ `metabase` ↔ `dashboard-mysql`；浏览器只访问 `/data_analysis/api/v1/embed-proxy/*` |
| 数据权限 | 只读账号仅授聚合库视图；组权限：仅已保存问题、禁下载、禁公开分享、禁订阅 |
| 管理 | admin 账号仅 API 使用（强随机密码 + 定期轮换），不与任何登录入口绑定；关闭 Public Sharing |
| 缓存 | 聚合表为预计算结果，Metabase 查询缓存 TTL 覆盖白天时段（首屏 ≤3s / 单图 ≤2s 达标手段之一） |

---

## 8. 前端模块设计（统一前端模式）

```text
frontend/src/modules/data_analysis/
├── api/            # auth/embed/alerts/dictionary 封装
├── views/          # Overview / DashboardShell(iframe) / AlertsCenter / Dictionary / AdminSources / AlertRules
├── components/     # KPI 卡、预警列表、字典表格
├── router.js       # 子路径路由 + 权限守卫（401→/data_analysis/auth/login，403→无权限页）
├── module.js       # 菜单元数据（仅展示，非鉴权）
└── README.md
```
- 路由权限按 `permissions` 渲染；iframe 加载 `/data_analysis/api/v1/embed-proxy/{token}`，失败统一 401/403 处理。

---

## 9. 接口清单（草案）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/data_analysis/auth/login` / `/auth/callback` / `/auth/logout` | 公开 | OIDC 契约端点 |
| GET | `/data_analysis/api/v1/auth/me` | 会话 | 身份/角色/权限/范围摘要 |
| GET | `/data_analysis/api/v1/embed/{dashboard_code}` | 对应 dashboard.*.view | 返回 `{token, expires_at}` |
| GET | `/data_analysis/api/v1/embed-proxy/{token}` | 令牌单次消费 | 代理 Metabase 渲染 |
| GET | `/data_analysis/api/v1/alerts` | alert.view | 预警中心列表（范围过滤） |
| POST | `/data_analysis/api/v1/alerts/{id}/ack` `.../close` | alert.manage | 预警闭环 |
| GET/PUT | `/data_analysis/api/v1/alert-rules` | alert.manage | 规则阈值（落平台 cfg） |
| GET | `/data_analysis/api/v1/dictionary` | dictionary.view | 指标字典（版本化） |
| GET/POST | `/data_analysis/api/v1/admin/sources` | aggregation.manage | 数据源状态/触发同步 |
| GET | `/data_analysis/healthz` | 公开 | 健康检查（含依赖状态） |

内部集成点：平台授权目录同步（machine Client）、审计 ingest（machine Client）、平台组织/任职数据（受控接口，待确认）、Metabase API（service 凭据，仅内网）。

---

## 10. 安全设计要点

1. **凭据**：跨库只读账号 + 各机器 Client Secret 一律 Secret 管理，不入 Git/日志/浏览器；`0600`。
2. **Metabase 收敛**：内网 + 代理唯一出口；admin 隔离；禁公开分享/禁下载。
3. **嵌入令牌**：短 TTL（≤5min）、单次消费、禁缓存头、绑定用户与范围；签发/消费全审计。
4. **审计留痕**：看板访问、预警配置变更、嵌入签发、同步触发（等保要求可追溯）。
5. **等保**：数据不出系统（无导出/无邮件/无公网 Metabase）；越权访问目标 = 0（健康指标）。
6. **上线前**：参照 `sharedDocs/01_架构设计/安全审查报告-2026-08.md` 流程做专项审查（跨库凭据、Metabase 暴露面、令牌生命周期、范围四层防线）。

---

## 11. 非功能与运维

| 类别 | 要求 | 落地 |
|---|---|---|
| 性能 | 首屏 ≤3s、单图 ≤2s | 聚合表 + Metabase 缓存 TTL；范围过滤在预计算层 |
| 可维护性 | 口径唯一字典、数据源集中配置 | 聚合表记录 `metric_dict_version`；`sync_source` 状态可见 |
| 可用性 | 看板不因同步失败回退旧数据 | 同步失败保留昨日数据 + 告警；Metabase 每日备份 |
| 兼容性 | Chrome/Edge 主流版本 | 前端模块沿用统一前端基线 |
| 监控 | healthz、日志脱敏 | 对齐平台结构化日志；敏感字段不落日志 |

---

## 12. 实施计划（含 PoC 修订）

| 阶段 | 内容 | 工期（估） | 依赖 |
|---|---|---|---|
| 0 | **PoC（修订后）**：V3 改为验证嵌入桥（签名嵌入+代理+参数注入）；新增 V5 验证聚合库+跨库同步 | 2–3 周 | 组织数据接口（OQ-D1）确认 |
| 1 | 平台接入骨架：onboard、OIDC 会话、目录发布、审计接入 | 1 周 | 阶段 0 结论 |
| 2 | 聚合库迁移 + aggregation-worker（同步/预计算） | 2–3 周 | 各子系统只读账号 |
| 3 | 嵌入桥 + Metabase 收敛（代理/缓存/禁下载） | 1.5–2 周 | 阶段 0/1 |
| 4 | 4 张看板 + 经营总览建模（口径按字典抽样核对） | 2–3 周 | 阶段 2/3 |
| 5 | 预警中心 + 指标字典 + 前端模块 | 1.5–2 周 | 阶段 1 |
| 6 | 口径验收（差异率 ≤1%）、安全审查、上线 | 1–1.5 周 | 全部 |

> 合计约 9–12 人周（不含 PoC），与既估一致；关键路径为"组织数据接口 → 嵌入桥 → 看板建模"。

---

## 13. 开放问题（Open Questions）

| # | 问题 | 责任方 | 状态 |
|---|---|---|---|
| OQ-D1 | 平台组织/任职数据获取方式：**已调研（2026-08-17）**——平台已存在 `GET /internal/owner-directory`（scope=`owner_directory.read`，onboard 自动创建 owner-directory 机器 Client，CRM ownerdirectory 为先例）；查询支持 keyword / user_id / role_codes 分页，响应为用户→组织列表；**但无组织树父子层级、无按组织查成员能力**，ORG_SUBTREE 需平台扩展或一期降级（见 OQ-D6） | 平台负责人 | **已调研（能力边界见 §4.3 注记）** |
| OQ-D2 | 跨库同步方式：SQL 增量拉取（水印）vs binlog CDC？ | 架构师/看板负责人 | PoC V5 定 |
| OQ-D3 | 嵌入形态：一期签名嵌入+代理（推荐）vs 二期用户同步+session token？ | 看板负责人 | PoC V3 定 |
| OQ-D4 | 只读副本是否升级（Q5 后续）：聚合库同步窗口是否足够？ | 架构师 | PoC 实测后定 |
| OQ-D5 | 预警阈值放平台 cfg 的粒度：每规则 vs 每阈值项？ | 产品/系统管理员 | 评审会定 |
| OQ-D6 | 看板口径中"销售本人及下属"的组织边界：按平台组织树全量下钻 vs 仅直接下属？ | 销售总监/PMO | 评审会定 |

---

## 14. 评审要点（提交评审会）

1. NG2 表述修订（聚合库）与"复用平台规则配置能力"修订（规则自建、配置托管）是否接受。
2. 应用登记参数（§3.1）与权限/角色 manifest（§3.4）确认。
3. OQ-D1/D6 需平台与业务侧当场闭环，否则行级范围无法编码。
4. 嵌入代理方案（浏览器不直连 Metabase）确认，含一期不做 Metabase 用户同步的取舍。
5. 跨库只读账号的运维契约（§5.4）确认责任分工。
