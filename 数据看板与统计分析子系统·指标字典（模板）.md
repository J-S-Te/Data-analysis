# 数据看板与统计分析子系统 · 指标字典（模板）

> **文档定位**：本文件是《数据看板与统计分析子系统需求规格说明书》的第六章（单列一章）的独立工作版。
> 用途：统一全公司看板指标的**业务定义与计算口径**，避免"同一张图、不同理解"的扯皮。
> **状态说明**：截至 2026-07-28 二次口径对齐会，**全部指标已确认锁定**（含新增 4.7 项目成本、新增 2.6 项目周期）。已确认指标开发须严格按本字典实现，禁止自行解读。
> **口径五要素**：①数据源 ②计算公式 ③统计周期 ④维度（筛选维度） ⑤排除项/口径说明。

---

## 0. 通用口径约定（全字典适用）

| 约定项 | 取值 | 确认状态 |
|---|---|---|
| 金额币种与含税 | 默认"人民币、含税"，指标名称须标注；如"签约金额（含税，万元）" | **已确认** |
| 时间基准 | 以"业务发生日/系统记录日"为准，非录入日；T+1 产出，统计日截至昨日 | **已确认** |
| 数据脱敏 | 不采集合同管理子系统的脱敏字段（如成本价）；不采集等保合规类详细数据 | 已定（调研5） |
| 统计单位 | 金额统一"万元"、比率统一"百分比"、时长统一"天/小时" | **已确认** |
| 历史口径变更 | 若口径调整，旧数据按旧口径、新数据按新口径，切换日标注，不回溯重算 | **已确认** |

---

## 1. 合同看板指标

### 1.1 签约金额
- **业务定义**：统计周期内**已生效**合同的签约总额，反映市场拿单规模。
- **数据源**：合同管理子系统（合同主表：金额、状态、生效日期）；客户区域来自客户与商机子系统（客户主数据）。
- **计算公式**：Σ（状态=已生效 且 生效日期∈统计周期 的合同金额）。框架合同计入首签总额，子订单不重复计入（避免虚高）。
- **统计周期**：默认按月/季/年，支持自定义区间。
- **维度**：签约月份、区域（省/市）、客户分类（业主/三方）、销售总监、销售。
- **排除项**：草稿/审批中/作废合同不计入；成本价等脱敏字段不采集。
- **确认状态**：**已确认**

### 1.2 签约金额趋势
- **业务定义**：签约金额随时间的变化曲线，识别淡旺季与增长态势。
- **数据源**：同 1.1。
- **计算公式**：按选定时间粒度（月/季）对 1.1 结果分组求和。
- **统计周期**：连续 N 个月/季，支持同比、环比。
- **维度**：时间粒度、区域、客户分类。
- **排除项**：同 1.1。
- **确认状态**：**已确认**

### 1.3 区域分布
- **业务定义**：签约金额/合同数在客户所在区域的分布，指导区域资源投放。
- **数据源**：合同子系统 + 客户主数据（区域）。
- **计算公式**：按区域分组，Σ签约金额 或 合同数计数；可切换"金额占比/数量占比"。
- **统计周期**：默认当年累计，可自定义。
- **维度**：省、市、客户分类。
- **排除项**：区域字段缺失的合同单列"未知区域"不参与排名。
- **确认状态**：**已确认**

### 1.4 转化率（商机→合同）
- **业务定义**：商机转化为正式合同的比例，衡量销售漏斗效率。
- **数据源**：客户与商机子系统（商机表：状态 线索/商机/中标/流失；合同关联）。
- **计算公式**：统计周期内"已中标且生成合同"的商机数 ÷ 同期进入"商机"阶段的商机总数 ×100%。
- **统计周期**：按月/季，支持漏斗分阶段展示（线索→商机→报价→中标）。
- **维度**：销售、区域、客户分类、商机来源。
- **排除项**：纯询价未建商机的不计入分母；流失商机明确标记原因（价格/技术/竞争对手）。
- **确认状态**：**已确认**

### 1.5 折扣分析
- **业务定义**：成交价相对标准报价的折让幅度，监控商务条款健康度。
- **数据源**：合同子系统（合同金额、标准报价/基准价）；**不涉及成本价**（脱敏不采集）。
- **计算公式**：折扣率 =（标准报价 − 合同成交价）÷ 标准报价 ×100%；按区间（0%/1-5%/5-10%/＞10%）分布。
- **统计周期**：按月/季。
- **维度**：销售总监、客户分类、区域。
- **排除项**：标准报价字段**已确认可采集**（符合脱敏策略，不涉及成本价）；草稿/作废合同不计。
- **确认状态**：**已确认（标准报价可采集）**

> 注：V1.1 中的"续签预测"按决策 D 调整为**规则预警**（见 PRD 第七章），不做预测模型，故不列入指标字典预测项。

---

## 2. 项目执行看板指标

### 2.1 项目状态分布
- **业务定义**：项目在各生命周期状态（未开始/执行中/现场完成/报告编制/审核中/已归档等）的数量与占比。
- **数据源**：项目服务内容管理子系统（项目主表：状态、计划日期）。
- **计算公式**：按状态枚举分组计数。
- **统计周期**：实时快照（T+1）。
- **维度**：团队、项目经理、状态、月份。
- **排除项**：已作废项目不计入。
- **确认状态**：**已确认**

### 2.2 交付周期（统计与趋势）  ← 原"准时交付率"经对齐会调整为交付周期
- **业务定义**：项目从实际开始到交付确认所历时长的统计分布与随时间的变化趋势，衡量交付效率与稳定性（替代原"准时交付率"指标）。
- **数据源**：项目服务内容管理子系统（实际开始日/计划开始日）+ 报告子系统（客户确认日）。
- **计算公式**：交付周期 = 客户确认日 − 项目实际开始日；展示平均 / P50 / P90，并按月份绘制趋势线。起始锚点默认"实际开始日"，若缺失则用"计划开始日"补位。
- **统计周期**：按月/季，趋势按月份。
- **维度**：**团队、项目经理、月份（趋势）**。
- **排除项**：开始日与确认日均缺失的项目不计；异常终止/作废项目单列，不计入常规分布。
- **确认状态**：**已确认**

### 2.3 返工率
- **业务定义**：发生退回/重做（报告退回或现场重测）的项目或任务比例。
- **数据源**：报告子系统（审核退回记录）+ 项目子系统（异常重测记录）。
- **计算公式**：发生退回/重做的报告数（或任务数）÷ 总报告数（或任务数）×100%。报告退回与现场重测**合并为一个指标**统计。
- **统计周期**：按月/季。
- **维度**：报告编制人、团队、项目类型。
- **排除项**：客户主动要求的小幅修改不计入"返工"。
- **确认状态**：**已确认**

### 2.4 人员利用率
- **业务定义**：实施工程师有效产能的利用程度。
- **数据源**：项目子系统（实施工程师 APP 签到/工时）+ 排班。**已确认工时/签到数据已结构化可采集。**
- **计算公式**：计费/有效工时 ÷ 应出勤工时 ×100%。"在路上/等待"**不计入**有效工时。
- **统计周期**：按月。
- **维度**：人员、团队、月份。
- **排除项**：无（数据可用性已确认）。
- **确认状态**：**已确认**

### 2.5 设备利用率
- **业务定义**：检测设备/仪器被项目占用的程度。
- **数据源**：设备能力管理（项目子系统）+ 设备申领记录。**已确认设备管理系统数据可采集。**
- **计算公式**：设备被占用时长 ÷ 可用总时长 ×100%。
- **统计周期**：按月。
- **维度**：设备类别、团队。
- **排除项**：无（数据可用性已确认）。
- **确认状态**：**已确认**

### 2.6 项目周期（签订→归档）  ← 2026-07-28 新增·已确认
- **业务定义**：项目从合同签订之日到报告归档所历时长（端到端），衡量项目从拿单到交付闭环的整体周期效率；区别于 2.2「交付周期」（仅执行段：实际开始→交付确认）。
- **数据源**：合同子系统（合同签订日/生效日）+ 报告子系统（报告归档日）。
- **计算公式**：项目周期 = 报告归档日 − 合同签订日；展示平均 / P50 / P90，并按月份绘制趋势线。
- **统计周期**：按月/季，趋势按月份。
- **维度**：**团队、项目经理、项目类型、月份（趋势）**。
- **排除项**：合同签订日或报告归档日任一缺失的项目不计；异常终止/作废项目单列，不计入常规分布。
- **确认状态**：**已确认（新增）**

---

## 3. 报告与质量看板指标

### 3.1 报告周期
- **业务定义**：从任务提交编制到报告签发的平均时长，衡量报告产出效率。
- **数据源**：报告子系统（编制开始时间、签发时间、版本记录）。
- **计算公式**：平均报告周期 = Σ（签发日 − 编制开始日）÷ 报告数；展示分位数（P50/P90）。
- **统计周期**：按月/季。
- **维度**：报告类型、团队、编制人。
- **排除项**：退回后重新计时按"首次提交到最终签发"总时长计。
- **确认状态**：**已确认**

### 3.2 首次通过率
- **业务定义**：报告一次通过全部审核（机审+人审）的比例，衡量报告一次质量。
- **数据源**：报告子系统（审核流程记录：机审结果、质量审核、授权签字）。
- **计算公式**：首次提交即"机审通过且人审通过无退回"的报告数 ÷ 总报告数 ×100%。机审与人审均计入；退回修改后再过算失败。
- **统计周期**：按月/季。
- **维度**：编制人、团队、报告类型。
- **排除项**：客户原因导致的补充不计入失败。
- **确认状态**：**已确认**

### 3.3 退回原因分布
- **业务定义**：报告被退回的原因归类分布，定位质量改进点。
- **数据源**：报告子系统（退回记录：退回环节、原因标签）。
- **计算公式**：按原因标签分组计数/占比（如：数据不符、格式问题、合规瑕疵、签名缺失）。
- **统计周期**：按月/季累计。
- **维度**：退回环节（机审/质量审核/签发）、团队。
- **排除项**：无。
- **确认状态**：**已确认**

### 3.4 客户投诉统计
- **业务定义**：客户通过门户提交的异议/投诉数量与处理状态。
- **数据源**：客户自助门户 / 服务评价（客户与商机子系统或报告子系统关联）。
- **计算公式**：投诉计数；按"未处理/处理中/已关闭"分状态；可算投诉关闭时长。
- **统计周期**：按月/季。
- **维度**：项目、客户、投诉类型。
- **排除项**：内部质检问题不计入客户投诉。
- **确认状态**：**已确认**

---

## 4. 财务与经营看板指标

### 4.1 回款率
- **业务定义**：实际收回款项占应收款项的比例，衡量资金回笼效率。
- **数据源**：结算与开票子系统（应收单、收款核销记录）。
- **计算公式**：已核销收款金额 ÷ 应收金额 ×100%（可按合同/项目/期间）。
- **统计周期**：按月/季/年。
- **维度**：销售、客户、项目、月份。
- **排除项**：未到收款节点的不算逾期；坏账需单列。
- **确认状态**：**已确认**

### 4.2 应收账款账龄
- **业务定义**：应收款项按逾期时长分档的分布，识别坏账风险。
- **数据源**：结算子系统（应收计划、实际回款、逾期天数）。
- **计算公式**：按账龄区间（0-30/31-60/61-90/＞90 天）分组 Σ未核销应收金额。
- **统计周期**：T+1 快照。
- **维度**：客户、销售、项目。
- **排除项**：已核销不计；预收款冲抵后净额计。
- **确认状态**：**已确认**

### 4.3 开票未回款 TOP10
- **业务定义**：已开票但未回款金额最高的前 10 笔，聚焦催收重点。
- **数据源**：结算子系统（发票记录、回款核销）。
- **计算公式**：筛选"已开票且未全额核销"，按未回款金额降序取前 10。
- **统计周期**：实时快照（T+1）。
- **维度**：客户、销售、开票日期。
- **排除项**：无。
- **确认状态**：**已确认**

### 4.4 部门产值  ✅ 已确认
- **业务定义**：按部门口径统计的产出价值。**口径按部门类型区分**：技术部门按项目子系统的"产值确认"金额统计；销售部门按结算子系统的"收入确认"金额统计。两者口径不同，不得跨类型混用。
- **数据源**：技术部门 → 项目服务内容管理子系统（产值确认）；销售部门 → 结算与开票子系统（收入确认）。
- **计算公式**：技术部门 Σ(项目产值确认)；销售部门 Σ(收入确认)；按部门类型分别取数、分别展示。
- **统计周期**：按月/季。
- **维度**：部门（区分技术/销售）、团队、月份。
- **排除项**：严禁跨部门类型混用口径；未归集到部门的公共产出不计入。
- **确认状态**：**已确认（技术按项目产值 / 销售按结算收入）**

### 4.5 个人产值  ✅ 已确认
- **业务定义**：按个人统计的产出贡献。**口径同 4.4，按个人所属部门类型区分**：技术岗个人按项目子系统的"产值确认"统计；销售岗个人按结算子系统的"收入确认"统计。
- **数据源**：同 4.4，关联人员字段。
- **计算公式**：技术岗个人 Σ(项目产值确认)；销售岗个人 Σ(收入确认)；按人员岗位类型分别取数。
- **统计周期**：按月/季。
- **维度**：个人、角色（技术/销售）、月份。
- **排除项**：人员归属/岗位变更的历史数据按归属时点划分；跨类型不混用。
- **确认状态**：**已确认（同 4.4）**

### 4.6 部门/个人成本  ✅ 已确认
- **业务定义**：与产出对应的成本投入（人力、报销、外包等），按部门与个人拆分。
- **数据源**：报销子系统（费用归集到项目/部门/人）+ 项目子系统（资源占用、人工分摊）。
- **计算公式**：Σ归集到该部门/人的报销费用 + 人力成本分摊（分摊规则：按项目工时占比分摊至部门/人）。
- **统计周期**：按月/季。
- **维度**：部门、个人、成本类型（报销/人工/设备）。
- **排除项**：未归集到具体部门/人的公共费用单列"未分摊"。
- **确认状态**：**已确认**

### 4.7 项目成本  ✅ 新增·已确认
- **业务定义**：单个项目对应的全部支出与成本，用于衡量项目投入与盈利性（项目级视角，区别于 4.6 的部门/人视角）。
- **数据源**：报销子系统（按项目归集的费用）+ 项目子系统（资源占用、设备使用、人工分摊至项目）。
- **计算公式**：Σ(该项目关联报销费用 + 设备/人工分摊成本)；可按"直接费用/分摊费用"分列。
- **统计周期**：按月/项目。
- **维度**：项目、团队、月份。
- **排除项**：未归集到具体项目的公共费用单列，不摊入单项目成本。
- **确认状态**：**已确认**

---

## 5. 字典维护规则
1. **新增/修改指标**：须走变更评审，更新"确认状态"与版本号，旧口径在备注留痕。
2. **版本**：本字典随 PRD 版本走；任何看板指标调整必须先改字典、再改看板。
3. **责任人**：每条指标指定"业务责任人（口径解释）"与"数据责任人（源确认）"。
4. **冻结**：已确认指标锁定，开发严格按字典实现，禁止开发自行解读口径。
5. **当前状态**：全部指标（1.1–4.7 含新增 2.6 项目周期）均已确认锁定，无待确认项。
