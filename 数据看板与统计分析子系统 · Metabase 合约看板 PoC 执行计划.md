# 数据看板与统计分析子系统 · Metabase 合同看板 PoC 执行计划（V1.0 修订版）

> **修订记录（2026-08-17）**：依据《基础平台接入与鉴权设计方案》（V0.1）阶段 0 修订——
> ① **V3 改为验证嵌入桥**（签名嵌入 + 代理收敛 + 范围参数注入），不再假设"iframe + 共享会话"（OSS Metabase 无 SSO/共享会话机制）；
> ② **新增 V5**：聚合库与跨库同步（跨 MySQL 实例无法视图 JOIN，需聚合落点）；
> ③ **补充前置项**：平台组织/任职数据接口（OQ-D1）确认；
> ④ **工期由 2 周调整为 3 周**（15 个工作日）。
>
> 配套文档：《需求规格说明书》（V1.3）附录 A（BI 选型 Metabase）；《只读副本替代方案（限流+夜间调度）》；《基础平台接入与鉴权设计方案》（V0.1）。
>
> 目标：用**合同看板**作为代表，在 3 周内验证"嵌入 Metabase 只读聚合库 + 跨库同步（T+1 预计算）+ 嵌入桥权限复用 + 无副本限流方案"五条主线的可行性，为正式开发是否全量铺开提供决策依据。

---

## 1. PoC 目标（要验证的 5 件事）

| # | 验证目标 | 对应 PRD/方案 |
|---|---|---|
| V1 | Metabase 可自托管并**只读查询聚合库**（非业务基表）取数 | 决策 A / §3.2 |
| V2 | 合同看板 5 项核心指标可在"聚合库 + Metabase"实现，且口径**严格符合指标字典 §5.1** | §4.1 / §5.1 |
| V3 | **嵌入桥**：签名嵌入 + 代理收敛 + 范围参数注入，行级权限复用基础平台角色 | 设计方案 §4.3–4.4 |
| V4 | **无只读副本**前提下，限流+夜间调度方案可将主库影响降到可接受 | Q5 / 替代方案文档 |
| V5 | **聚合库与跨库同步**：夜间窗口内完成维表镜像 + 预计算，白天只命中聚合库 | 设计方案 §5 |

---

## 2. 非目标（PoC 不做）

- 不全量实现 4 张看板（仅合同看板作代表，覆盖 SELECT/聚合/下钻/预警典型场景）。
- 不做 ML / 预测类（续签预测已移除，仅验证规则高亮）。
- 不做移动端（NG3）。
- 不验证导出/邮件（NG5，本期不做）。
- 不接入全部 6 个子系统（仅合同 + 客户主数据 + 商机相关库 + 聚合库）。
- **一期不做 Metabase 用户同步**（采用签名嵌入 + 代理；用户同步列为二期候选，OQ-D3）。

---

## 3. 环境与前置

| 项 | 要求 |
|---|---|
| Metabase | 开源版，Docker 自托管，资源 2C4G 起 |
| 聚合库 | **dashboard-mysql**（测试实例，MySQL 8.4）：维表镜像 + 预计算聚合表（schema 草案见设计方案 §5.2） |
| 业务库 | **测试 / 预发环境**只读账号（合同库、客户与商机库，必要时项目库）；严禁直连生产主库做 PoC |
| 基础平台 | 测试环境：OIDC/Keycloak、授权目录、审计 ingest 可用 |
| 组织数据 | 平台 owner-directory 接口已确认存在（`GET /internal/owner-directory`，scope=`owner_directory.read`）；**无组织树/成员下钻能力**（OQ-D1 能力边界见设计方案 §4.3 注记），PoC 用模拟组织数据验证范围注入逻辑 |
| 数据 | 脱敏样本数据，量级尽量贴近生产（必要时放大模拟） |
| 口径基线 | 指标字典 §5.1 已锁定（签约金额/趋势/区域/转化率/折扣分析） |

---

## 4. 工作分解（3 周 / 15 个工作日）

### 第一周：连通 + 聚合库建模 + 同步
- **D1–D2 环境搭建**
  - 部署 Metabase（Docker）；创建聚合库 dashboard-mysql（测试实例）。
  - 创建业务库**只读账号**（仅 SELECT 看板所需表）；按替代方案 §3.1 配置连接池上限、语句超时（中间代理层）。
  - 连通性测试；聚合库 ↔ Metabase ↔ 业务库网络验证。
- **D3–D5 聚合库建模 + 跨库同步（V5 主线）**
  - 按字典 §5.1 建聚合库 schema：dim_customer / dim_project / fct_contract_signing 等（设计方案 §5.2 草案）。
  - 实现跨库同步（合同库 + 客户主数据 + 商机）：维表镜像 + 预计算聚合；验证增量水印与幂等（重复运行结果一致）。
  - Metabase 连接聚合库，建 Models / Questions 实现 5 个指标 + 全局筛选器（时间/区域/客户分类/销售）。

### 第二周：嵌入桥 + 权限 + 夜间调度
- **D6–D7 嵌入桥验证（V3，对齐设计方案 §4.3）**
  - 启用 Metabase 签名嵌入；实现 dashboard-api 代理（/embed-proxy 单次消费、TTL ≤5min）。
  - 角色 → 范围参数注入（TENANT / ORG_SUBTREE / TEAM，模拟组织数据）。
  - 越权 403、禁下载、审计上报验证。
- **D8–D9 夜间调度验证（V5/V4）**
  - 配置 cron / Temporal：夜间 1:00–06:00 跨库同步 + 预计算。
  - 白天验证看板只命中聚合库（缓存命中率、无业务基表查询）。

### 第三周：主库影响 + 验收 + 报告
- **D10–D11 主库影响实测（V4）**
  - 对比夜间批与白天看板时段主库 QPS / IOPS / 慢查询；验证限流参数是否生效、有无交易抖动。
- **D12–D13 口径核对 + 范围回归**
  - 财务/销售责任人对照字典 §5.1 抽样核对 5 项指标（差异率 ≤1%）；范围注入正确性回归。
- **D14 验收与报告**
  - 对照 §8 评估标准判定成功/失败，输出《PoC 验证报告》与决策建议书。
- **D15 Buffer / 问题修复**。

---

## 5. 合同看板指标落地映射（口径在聚合库实现，Metabase 只查聚合库）

| 指标 | 聚合库实现 | Metabase 实现方式 | 口径校验点（必须一致） |
|---|---|---|---|
| 签约金额 | fct_contract_signing：Σ(状态=已生效)金额 | 查聚合表 | 排除草稿/审批中/作废；框架合同计首签、子订单不重复 |
| 签约金额趋势 | 按月份分组 + 同比/环比 | 查聚合表 | 时间基准=业务发生日，T+1 截至昨日 |
| 区域分布 | 按客户主数据区域分组（关联 dim_customer） | 查聚合表，可切金额/数量 | 区域缺失单列"未知" |
| 转化率 | 商机 + 合同在聚合库 JOIN 后计算 | 查聚合表 | 纯询价不计分母；支持分销售/区域 |
| 折扣分析 | 折扣率=(标准报价−成交价)/标准报价 | 查聚合表，区间分布 | 标准报价已确认可采集；不涉及成本价 |
| 续签到期预警 | 规则高亮（复用合同子系统台账） | 参数/状态字段 | 到期前 60/30/10 天；看板标记，不邮件 |

> **关键差异（V1.0 修订）**：上一版"在数据库建只读视图封装 JOIN"改为"在聚合库预计算"——合同库与客户/商机库为**独立 MySQL 实例**（已核实 compose 拓扑），MySQL 视图无法跨实例，Metabase Question 无法跨库 JOIN。这也是 V5 存在的原因。
>
> 校验方式：每个指标由财务/销售责任人对照字典抽样核对，差异率 ≤1%（对齐 §8 健康指标）。

---

## 6. 嵌入桥与权限复用验证清单（V3，对齐设计方案 §4.3）

- [ ] 签名嵌入启用；浏览器只访问 `/data_analysis/api/v1/embed-proxy/{token}`，不接触 Metabase 地址与凭据。
- [ ] 令牌生命周期：短 TTL（≤5min）、单次消费、绑定用户与范围；过期/复用一律 403。
- [ ] 角色映射正确：老板/销售总监可见合同看板；非授权角色返回 403 / 不可见。
- [ ] 范围参数注入正确：销售总监仅见本人及下属（ORG_SUBTREE）；老板全量（TENANT）。
- [ ] 下钻至"合同列表（聚合级）"不含脱敏/合规字段（验证 NG6）。
- [ ] 看板访问、令牌签发**操作留痕**写入基础平台（audit.ingest）。

---

## 7. 限流 + 夜间调度 + 聚合库验证清单（V4 / V5）

- [ ] 只读账号仅 `SELECT`，且限定看板所需表；连接池上限 + 语句超时（30s）生效，单条跑飞查询被中断。
- [ ] 夜间批在窗口（1:00–06:00）内完成**全部同步 + 预计算**。
- [ ] 同步幂等：重复运行结果一致；失败重试后状态可查（sync_source）。
- [ ] 白天看板查询**只命中聚合库**（缓存命中率 ≥ 95%，无业务基表查询）。
- [ ] 主库在白天看板时段 QPS / IOPS **无异常上升**（增幅 <5%，无交易抖动）。

---

## 8. 评估标准（成功 / 失败判据）

**成功（建议正式采用 Metabase + 聚合库方案，进入全量开发）**
- 5 项指标可渲染且口径与字典抽样一致（差异率 ≤1%）。
- 嵌入桥（V3）全部通过：令牌生命周期、范围注入、越权 403、禁下载、审计。
- 聚合库 + 限流方案（V4/V5）通过：白天主库影响 ≤ 阈值，首屏 ≤3s、单图 ≤2s。
- 业务人员（销售总监角色）可自助筛选/下钻，无需开发介入。

**失败（需回退或调整）**
- 口径无法在 Metabase 表达（需大量自定义 SQL 且性能差）→ 回退评估 Superset。
- 嵌入桥范围注入不可控或令牌安全不达标 → 重新评估嵌入方案（含 Metabase 用户同步）。
- 跨库同步在夜间窗口内无法完成或数据一致性不达标 → 评估 CDC / binlog 方案（OQ-D2）。
- 主库压力不可接受 → 触发 Q5 只读副本立项，本方案逻辑平移至副本。

---

## 9. 风险与回退

| 风险 | 缓解 / 回退 |
|---|---|
| 测试环境无生产级数据量 | 用脱敏样本 + 量级放大模拟；重点看相对波动而非绝对值 |
| 开源版无 Persisted Models | 用聚合库预计算表替代（本就是本方案主路径，不构成阻塞） |
| 平台组织数据接口未就绪（OQ-D1） | 先用模拟组织数据验证范围注入逻辑；接口契约确认后替换 |
| 跨库同步吞吐不足 | 延长窗口 / 并行任务 / 评估 CDC（OQ-D2） |
| 视图建模口径与字典偏差 | D3–D5 由责任人逐指标核对，偏差不过夜 |
| 基础平台审计/目录接口未就绪 | 先行本地 mock，联调期替换 |

---

## 10. 产出物

1. **PoC 环境**：Metabase 实例 + 聚合库（dashboard-mysql）+ 嵌入桥 + 合同看板 5 图（含筛选/下钻/预警高亮）。
2. **《PoC 验证报告》**：含主库影响实测数据、嵌入桥与范围注入结果、口径核对记录。
3. **决策建议书**：
   - 是否正式采用 Metabase；
   - 嵌入形态定案（签名嵌入+代理 vs 用户同步）；
   - 聚合库同步方案定案（SQL 增量 vs CDC）；
   - 是否仍需只读副本（Q5）；
   - 是否进入 4 张看板全量开发。

---

## 11. 决策依赖

- **Q5 只读副本**：本 PoC 默认按"无副本 + 聚合库 + 替代方案"验证；结论决定是否需要升级只读副本。
- **OQ-D1 组织数据接口**：接口已确认存在（owner-directory）；PoC 验证范围注入时确认其能力边界（无组织树），并给出 ORG_SUBTREE 一期实现建议（设计方案 §4.3 注记）。
- **OQ-D3 嵌入形态**：本 PoC 默认签名嵌入 + 代理；结论决定二期是否做 Metabase 用户同步。
- **OQ-D2 同步方式**：本 PoC 默认 SQL 增量拉取（水印）；结论决定是否切换 CDC。

---

*文档状态：V1.0 修订稿（待排期启动）*
